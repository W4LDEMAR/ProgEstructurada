#include <stdio.h>
#include <stdlib.h>

#define maxpal 20
#define maxjug 5
#define maxint

void impresionTurno(char);
int buscarLetra(char, char);
int turno(char);

int main(){
    char palabras[][maxpal] = {"programa", "dinosaurio", "telefono", "ingenuo", "refrigerador", "renacuajo", "mantenimiento",
    "resultados", "coronavirus", "alpinismo", "caballo", "hermano", "contaminar", "hormonas", "columpio", "ambulancia", "hormiguero",
    "imprimir", "nostalgia", "literatura"};
    char juego[maxjug][maxint][], temp[];
    return 0;
}
